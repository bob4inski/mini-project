services:
  backend:
    build:
      context: ./app
      dockerfile: Dockerfile
    networks:
      - cats

  nginx:
    image: nginx
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - /var/log/cats:/var/log/nginx
    networks:
      - cats
    ports:
      - 80:80
    depends_on:
      - backend

networks:
  cats:
